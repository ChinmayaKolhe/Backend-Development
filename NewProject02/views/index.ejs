<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            class="block w-full rounded-lg px-5 py-3 bg-cyan-400"
            type="text" name="title"
            placeholder="Title Goes Here"
          />
          <textarea
            class="block w-full rounded-lg px-5 py-3 bg-green-300 resize-none mt-3" name="details"
            placeholder="Write your task details here"
          ></textarea>
          <input
            class="mt-4 bg-blue-500 rounded-lg px-6 py-2 text-white"
            type="submit"
            value="Add Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-3 flex-wrap">
        <% if(files.length>0){ %> 
            <% files.forEach(function(val){ %>
        <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter">
            <%= val %>
          </h1>
          <div class="flex mt-4 w-full justify-content gap-4 items-center">
            <a class="text-blue-500 inline-block" href="/file/<%= val %>">Read More.</a>
            <a class="text-zinc-400" href="/edit/<%= val %>">Edit</a>
            <a class="text-zinc-400" href="/remove/<%= val %>">Remove</a>

          </div>
        </div>

            <% }) %> 
        <% } else { %>
        <h3 class="text-zinc-500">No Tasks Yet...</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
