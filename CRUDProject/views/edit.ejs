<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
     <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    
</head>
<body class="bg-zinc-900">
<div class="w-full h-full bg-zinc-900 p-10 text-white">
<div class="nav mb-10">
    <a class="text-blue-500" href="/read">Read Users</a>
</div>

    <h1 class="text-3xl tracking-tighter mb-3">Update User</h1>
<form action="/update/<%= user._id %>" method="post">
    
    <input id="name" name="name" class="px-5 py-3 bg-transparent border-2 border-zinc-800 outline-none rounded-lg w-80 mb-4 mt-10" type="text" placeholder="Enter Name" value="<%= user.name %>" required>

    <input id="email" name="email" class="px-5 py-3 bg-transparent border-2 border-zinc-800 outline-none rounded-lg w-80 mb-4 mt-7" type="email" placeholder="Enter Email" value="<%= user.email %>" required>

    
    <input id="imageUrl" name="imageUrl" class="px-5 py-3 bg-transparent border-2 border-zinc-800 outline-none rounded-lg w-80 mb-4 mt-7" type="text" value="<%= user.imageUrl %>" placeholder="Image URL">

    <input class="px-5 py-3 bg-yellow-500 text-black rounded-md mt-5" type="submit" value="Update User">
</form>

</div>
</body>
</html>